// Import de la fonction `createContext` depuis la bibliothèque React.
import { createContext } from 'react';

// Création d'un contexte nommé `UidContext` à l'aide de la fonction `createContext`.
// Ce contexte sera utilisé pour stocker et partager l'ID utilisateur (UID) dans l'application.
export const UidContext = createContext();


